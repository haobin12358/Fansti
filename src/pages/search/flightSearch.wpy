<style scoped lang="less">
    @import "../../styles/card";
    @import "../../styles/table";

    .container {
      .container-card {
        background: #a1afba;

        .card-title{
          background: #ffffff;
        }

        .standard-table{
          .thead{

            .fz24();

            .tr{
              .td{
                padding: 11rpx 0;
              }

              .td.td-narrow{
                flex: .5;
              }
            }
          }

          .tbody{

            .fz21();

            .tr{
              background: @mainBlueColor;
              color: #ffffff;

              &:nth-child(even){
                background: #a1afba;
              }

              .td{
                padding: 11rpx 0;

              }

              .td.td-narrow{
                flex: .5;
              }
            }
          }
        }

      }
    }
</style>

<template>
  <view class="container" style="height: {{containerHeight}}px;">
    <scroll-view scroll-y class="container-card" style="height: {{cardHeight}}px;">
      <view class="card-title">
        航班时刻查询
      </view>

      <view class="standard-table">

        <view class="thead">
          <view class="tr">
            <view class="td td-narrow">目的地</view>
            <view class="td td-narrow">航班号</view>
            <view class="td">航班</view>
            <view class="td td-narrow">机型</view>
            <view class="td">起飞时间</view>
            <view class="td">到达时间</view>
          </view>
        </view>
        <view class="tbody">
          <view class="tr" wx:for="{{res}}" wx:key="flight">
            <view class="td td-narrow">{{item.dest}}</view>
            <view class="td td-narrow">{{item.flight}}</view>
            <view class="td">{{item.mydate}}</view>
            <view class="td td-narrow">{{item.aircraft}}</view>
            <view class="td">{{item.etd}}</view>
            <view class="td">{{item.eta}}</view>
          </view>
        </view>

      </view>
    </scroll-view>
  </view>


</template>

<script>
  import wepy from 'wepy';
  import CardMixin from '@/mixins/cardMixin';

  export default class FlightSearch extends wepy.page {
    config = {
      navigationBarTitleText: '航班时刻查询'
    }

    components = {};

    mixins = [CardMixin]

    data = {
      res: []
    };
    computed = {};

    methods = {};

    events = {};

    onLoad(params, data) {
      this.res = data.preload.res
    }
  }
</script>

